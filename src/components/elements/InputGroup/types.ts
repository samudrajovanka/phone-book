export type InputGroupProps = {
  children: React.ReactNode;
  label?: string;
  id?: string;
  isBlock?: boolean;
  errorMessage?: string;
};

export type InputGroupStyleProps = {
  $isBlock?: boolean;
}